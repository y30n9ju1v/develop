---
title: "맺음말"
date: 2026-08-02T00:00:00+09:00
draft: false
tags: ["computer-vision", "multiview-geometry", "book"]
categories: ["books"]
description: "5부 14장을 거쳐온 멀티뷰 기하학 여정을 돌아보고, 이 책이 다루지 않은 것과 이 시리즈의 다음 책이 어디서 이어받는지를 정리하는 맺음말"
---

// tag::body[]
_link:../[전체 목차] · 이전: link:../14-measuring-reconstruction-quality/[14장: 재구성 품질을 재는 법]_

'''

== 지나온 길

1부에서는 카메라 한 대의 기하학을 다졌습니다 — 핀홀 모델이 3차원을 2차원으로 짓누르며 깊이를 지운다는 것, 외부 파라미터가 세계 좌표계와 카메라 좌표계를 잇는다는 것, 그리고 실제 렌즈의 왜곡을 모델링하고 되돌리는 법까지.

2부에서는 두 번째 사진이 그 지워진 깊이를 어떻게 되살리는지 봤습니다 — 에피폴라 기하학의 제약, 본질행렬을 분해해 카메라의 상대 자세를 복원하는 법(그리고 단안 재구성은 스케일을 알 수 없다는 근본적 한계), 삼각측량과 그 오차가 깊이에 따라 어떻게 달라지는지까지.

3부에서는 두 장을 넘어 여러 장으로 확장했습니다 — 특징점을 검출하고 매칭하는 법, 점진적으로 카메라를 추가하는 SfM 파이프라인, 그리고 모든 걸 한꺼번에 다듬는 번들 조정에서 스케일 게이지를 고정하지 않으면 재투영 오차는 낮아도 완전히 틀린 결과가 나온다는 걸 직접 겪어봤습니다.

4부에서는 이 과정을 실시간으로 돌리는 SLAM을 다뤘습니다 — 오도메트리를 이어붙이면 드리프트가 쌓인다는 것, 포즈 그래프 최적화가 나무 구조에서는 아무것도 개선하지 못한다는 뜻밖의 사실, 그리고 루프 클로저 간선 단 하나가 그래프에 순환을 만들어 궤적 전체의 오차를 재분배해준다는 것까지.

5부에서는 이 모든 결과가 3D Gaussian Splatting의 입력으로 어떻게 이어지는지, 그리고 "카메라를 더 쓰면 무조건 좋아지는가"라는 질문에 "관측을 실제로 활용해야만 좋아진다"고 답하며 이 책을 마무리했습니다.

== 이 책이 다루지 않은 것

이 책은 멀티뷰 기하학의 고전적인 뼈대에 집중했습니다. 의도적으로 남겨둔 것들이 많습니다 — 실전에서 쓰이는 강건한 추정 기법(RANSAC으로 이상치를 거르는 법, 4장에서 이상치가 에피폴라 제약을 어긴다는 것만 확인했지 실제로 그 이상치들을 자동으로 걸러내는 절차는 다루지 않았습니다), SIFT/ORB 같은 실전 특징 기술자의 구체적인 구현, 스테레오 매칭과 깊이 맵 추정, 그리고 라이다 같은 다른 센서와의 융합입니다. 이들은 이 책에서 다진 카메라 기하학과 최적화의 기본 언어 위에서 자연스럽게 확장해서 배울 수 있는 것들입니다.

== 이 시리즈의 다음 책이 이어받는 지점

13장에서 예고했듯, 이 시리즈의 다음 책은 이 책이 만든 카메라 자세와 점군을 입력으로 받아, 미분가능 렌더링으로 실제 3D Gaussian Splatting 장면을 학습하는 과정을 다룹니다. 이 책에서 배운 재투영 오차라는 개념이, 사실 3DGS 학습의 손실 함수와 거의 같은 뿌리를 공유한다는 걸 다음 책에서 다시 만나게 될 것입니다 — 3DGS는 "가우시안을 렌더링한 이미지"와 "실제 사진" 사이의 오차를 최소화하는데, 이건 이 책의 재투영 오차를 "점 하나"가 아니라 "이미지 전체"로 확장한 것과 다르지 않습니다.

그리고 그 뒤를 잇는 책은, 이렇게 재구성된 디지털 트윈 위에서 자율주행 E2E 스택을 검증하는 방법론을 다룹니다 — 이 책의 서문에서 던진 원래 질문, "3DGS로 만든 디지털 트윈이 왜 검증 근거로 신뢰할 만한가"에 대한 답은, 결국 이 책에서 확인한 사실들(스케일 게이지, 드리프트, 재투영 오차)이 3DGS 재구성 품질에 얼마나 정직하게 반영되는지에 달려 있습니다.

== 다음으로

이 책에서 그린 그림에 살을 붙이고 싶다면, 실제 COLMAP(오픈소스 SfM 도구)의 소스나 출력을 직접 열어보는 걸 권합니다 — 이 책에서 손으로 구현한 카메라 리셋션, 번들 조정, 게이지 고정이 실전 도구에서 어떻게 훨씬 정교한 형태로 구현되어 있는지 확인할 수 있습니다. 그런 다음엔 실제 사진 몇 장(스마트폰으로 건물 하나를 빙 둘러 찍은 사진들도 충분합니다)으로 COLMAP을 직접 돌려보고, 이 책에서 배운 지표(재투영 오차, 트랙 길이)가 실전 데이터에서는 어떤 값으로 나오는지 비교해보는 것도 좋은 다음 걸음입니다.

멀티뷰 기하학을 "이해했다"는 건 공식을 외웠다는 뜻이 아니라, 재구성 결과가 이상할 때 "카메라 모델이 잘못됐나, 대응점이 틀렸나, 스케일이 어긋났나, 드리프트가 쌓였나"를 순서대로 짚어볼 수 있게 되는 것입니다. 이 책이 그 진단 능력의 첫걸음을 놓아줬다면, 그것으로 충분합니다.
// end::body[]
