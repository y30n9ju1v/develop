---
title: "14. 언어를 넘어서: Python asyncio와 TypeScript Promise"
date: 2026-08-17T00:00:00+09:00
draft: false
tags: ["functional-programming", "typescript", "python", "async", "book"]
categories: ["books"]
description: "이 책이 파이썬으로 배운 Functor/Monad/Applicative 패턴이 TypeScript의 Promise에서도 그대로 발견된다는 것을, then/flatMap 대응·Promise.all의 Applicative적 성격·구조적 동시성의 부재까지 실제 컴파일·실행으로 확인하는 14장"
---

// tag::body[]
_link:../[전체 목차] · 이전: link:../13-algebra-and-automaton/[13장: 대수와 오토마타: 두 개의 렌즈로 다시 보는 비동기]_

'''

== 인트로: 13장까지가 파이썬 이야기였다면

이 책은 지금까지 파이썬을 중심으로 이야기했습니다. 의존적인 `await` 흐름을 Monad의 `flatMap` 관점으로 읽는 법, `asyncio.gather`를 독립 작업 결합의 Applicative 관점으로 읽는 법, `TaskGroup`의 구조적 동시성을 살폈습니다. 이 대응은 API의 완전한 동일성이 아니라 설계를 이해하는 렌즈입니다. 이 장에서는 그 렌즈가 TypeScript에서도 어디까지 유효한지 확인합니다.

2장이 콜백 지옥의 원조로 지목한 언어는 사실 파이썬이 아니라 JavaScript였습니다 — Node.js의 콜백 기반 API들이 콜백 지옥이라는 용어 자체를 유명하게 만들었고, `Promise`도 파이썬의 `asyncio`보다 먼저(ES2015, 2015년) 표준이 됐습니다. 이 장은 그 JavaScript(정확히는 타입까지 확인할 수 있는 TypeScript)로 지금까지 배운 패턴을 다시 확인합니다 — 새 개념을 배우는 장이 아니라, **이미 아는 개념이 언어를 갈아 끼워도 그대로 성립하는지 검증하는 장**입니다.

== `.then`은 `flatMap`과 어디까지 닮았나

4장에서 `flatMap`의 핵심 모양은 "컨테이너 안의 값에 `T -> Container<U>` 함수를 적용했을 때, `Container<Container<U>>`가 아니라 `Container<U>`로 한 겹 평탄화하는 것"이었습니다. TypeScript의 `Promise.prototype.then`은 반환된 Promise/thenable을 채택(resolve)하므로 이 모양과 닮았습니다.

[source,typescript]
----
function fetchUser(id: number): Promise<{ id: number; name: string }> {
  return Promise.resolve({ id, name: `user-${id}` });
}

function fetchOrders(userId: number): Promise<string[]> {
  return Promise.resolve([`order-${userId}-1`, `order-${userId}-2`]);
}

async function main() {
  // .then에 넘긴 콜백이 Promise를 반환하면, 결과는 Promise<Promise<T>>가 아니라
  // Promise<T>로 한 겹 평탄화된다 -- flatMap과 닮은 부분이다.
  const orders = await fetchUser(1).then((user) => fetchOrders(user.id));
  console.log("orders:", orders);

  // async/await도 같은 의존 관계를 선형적으로 표현한다.
  const user = await fetchUser(2);
  const orders2 = await fetchOrders(user.id);
  console.log("orders2 (await 버전):", orders2);
}

main();
----

[source]
----
$ npx tsc --strict --target es2020 --module commonjs then_flatmap.ts && node then_flatmap.js
orders: [ 'order-1-1', 'order-1-2' ]
orders2 (await 버전): [ 'order-2-1', 'order-2-2' ]
----

`fetchUser(1).then(user => fetchOrders(user.id))`의 콜백은 `Promise<string[]>`을 반환합니다. 반환 Promise는 다음 Promise가 채택하므로 `orders`는 `string[]`이 됩니다. 이 평탄화와 의존적 체이닝은 `.then`을 `flatMap`/bind와 비교할 수 있는 강한 근거입니다. 다만 Promise는 즉시 시작될 수 있고, 거부·thenable 채택·마이크로태스크 순서 같은 관찰 가능한 의미론이 있으므로, 수학적 Monad 법칙을 만족하는 구현이라고 단정하지는 않습니다. `await` 역시 `.then`의 단순한 문법 포장이 아니라 awaitable/thenable의 결과를 기다리는 언어 문법입니다. 두 경우 모두 의존 관계를 중첩 없이 표현한다는 공통점을 확인하는 것이 이 절의 목적입니다.

== `Promise.all`은 Applicative와 어떤 관계인가

6장에서 `asyncio.gather`를 Applicative 관점으로 읽은 근거는 "서로 의존하지 않는 작업들을 함께 시작하고 결과를 모은다"는 것이었습니다. `Promise.all`도 독립 Promise의 결과를 모으는 API입니다. 다만 이는 동시 작업 결합이라는 직관의 대응이지, 모든 예외·취소·실행 시점까지 같은 계약이라는 뜻은 아닙니다.

[source,typescript]
----
function delay<T>(ms: number, value: T): Promise<T> {
  return new Promise((resolve) => setTimeout(() => resolve(value), ms));
}

async function main() {
  const start = Date.now();
  // 세 작업 모두 즉시 시작된다 -- 순차로 await하면 300ms, all이면 100ms.
  const [a, b, c] = await Promise.all([
    delay(100, "a"),
    delay(100, "b"),
    delay(100, "c"),
  ]);
  console.log(`Promise.all: [${a}, ${b}, ${c}] in ${Date.now() - start}ms`);

  // Python asyncio.gather(return_exceptions=True)에 대응하는 건 allSettled다.
  // all은 하나라도 실패하면 즉시 reject하고 나머지 결과는 버린다.
  const settled = await Promise.allSettled([
    delay(20, "ok"),
    Promise.reject(new Error("boom")),
  ]);
  console.log("allSettled:", settled.map((r) => r.status));
}

main();
----

[source]
----
$ npx tsc --strict --target es2020 --module commonjs applicative_all.ts && node applicative_all.js
Promise.all: [a, b, c] in 101ms
allSettled: [ 'fulfilled', 'rejected' ]
----

100ms짜리 작업 세 개를 각각 만든 뒤 `Promise.all`에 넘기면 약 100ms에 끝납니다. 이미 시작된 세 Promise의 대기 시간이 겹쳤다는 뜻입니다. 그리고 5장에서 다룬 `asyncio.gather(return_exceptions=True)`(부분 실패를 허용하고 전부 모으는 방식)에 더 가까운 API는 `Promise.all`이 아니라 `Promise.allSettled`입니다. `Promise.all`은 하나가 거부되면 즉시 전체를 reject하지만, 나머지 연산을 취소하지는 않습니다.

== 구조적 동시성: TypeScript에는 `TaskGroup`이 없다

지금까지는 "파이썬에 있는 게 TypeScript에도 있다"는 이야기였습니다. 이번엔 반대입니다 — 6장에서 다룬 Python 3.11 `TaskGroup`(자식 태스크 하나가 실패하면 나머지를 **자동으로 취소**하는 구조적 동시성)에 대응하는 게 TypeScript/JavaScript에는 없습니다. 실측으로 그 차이를 봅니다.

[source,typescript]
----
function delay(ms: number, label: string): Promise<string> {
  return new Promise((resolve) => {
    setTimeout(() => {
      console.log(`  [백그라운드] ${label} 완료 (취소되지 않음!)`);
      resolve(label);
    }, ms);
  });
}

async function main() {
  console.log("Promise.all 시작");
  try {
    await Promise.all([
      delay(50, "slow-task"),
      Promise.reject(new Error("fast failure")),
    ]);
  } catch (e) {
    console.log(`await 즉시 반환: ${(e as Error).message}`);
  }
  console.log("main() 종료 -- 그런데 slow-task는 아직 안 끝났다");
  await delay(80, "wait-to-observe");
}

main();
----

[source]
----
$ npx tsc --strict --target es2020 --module commonjs structured_gap.ts && node structured_gap.js
Promise.all 시작
await 즉시 반환: fast failure
main() 종료 -- 그런데 slow-task는 아직 안 끝났다
  [백그라운드] slow-task 완료 (취소되지 않음!)
  [백그라운드] wait-to-observe 완료 (취소되지 않음!)
----

`fast failure`가 즉시 `Promise.all`을 reject시키고 `main()`의 `catch`가 실행되지만, `slow-task`는 취소되지 않고 백그라운드에서 계속 돌다가 `main()`이 이미 "종료"라고 찍은 뒤에 완료됩니다. 이것은 연산이 반드시 누수라는 뜻은 아니지만, 호출자가 실패 뒤의 수명·부수 효과를 관리해야 한다는 중요한 차이입니다. 같은 상황을 파이썬 `TaskGroup`으로 다시 만들어보면 대조가 분명해집니다.

[source,python]
----
import asyncio

async def slow_task():
    try:
        await asyncio.sleep(0.05)
        print("  [백그라운드] slow-task 완료")
    except asyncio.CancelledError:
        print("  [백그라운드] slow-task 취소됨 (TaskGroup이 자동으로 취소)")
        raise

async def fast_failure():
    await asyncio.sleep(0)
    raise ValueError("fast failure")

async def main():
    print("TaskGroup 시작")
    try:
        async with asyncio.TaskGroup() as tg:
            tg.create_task(slow_task())
            tg.create_task(fast_failure())
    except* ValueError as eg:
        print(f"ExceptionGroup 수신: {[str(e) for e in eg.exceptions]}")
    print("main() 종료")

asyncio.run(main())
----

[source]
----
$ python3 taskgroup_check.py
TaskGroup 시작
  [백그라운드] slow-task 취소됨 (TaskGroup이 자동으로 취소)
ExceptionGroup 수신: ['fast failure']
main() 종료
----

`fast_failure`가 예외를 던지자마자 `TaskGroup`은 `slow_task`에 취소를 요청하고, 취소 지점에서 `CancelledError`가 전달됩니다. 자식이 취소를 삼키거나 취소 지점에 도달하지 않으면 즉시 멈추지 않을 수 있으므로, "자동 취소"도 협력적이라는 점은 기억해야 합니다. 그래도 "main() 종료" 전에 모든 자식을 기다리고 예외를 묶어 전파한다는 보장은 큽니다. TypeScript/JavaScript에서는 `AbortController`와 `AbortSignal`을 각 API에 배선해 비슷한 효과를 만들 수 있지만, Promise 자체가 이를 강제하지는 않습니다. 이것이 `TaskGroup`이 제공하는 구조적 동시성의 핵심입니다.

== 정리: 패턴은 언어를 넘어서지만, 보장 수준은 아니다

이 장이 실제로 컴파일하고 실행해 확인한 것은 세 가지입니다.

* **`.then`은 `flatMap`/bind와 닮았다** — 반환 Promise를 채택해 `Promise<Promise<T>>` 대신 `Promise<T>`로 한 겹 평탄화합니다. 다만 Promise의 실행·거부 의미론까지 수학적 Monad와 같지는 않습니다.
* **`Promise.all`은 독립 작업 결합의 Applicative 직관을 보여 준다** — 이미 만든 Promise들을 함께 기다리고, 전부 성공해야 결과가 있습니다. "부분 실패 결과를 전부 수집"하는 API는 `Promise.all`이 아니라 `Promise.allSettled`입니다.
* **구조적 동시성은 언어 계약이지, 패턴 자체의 필연적 귀결이 아니다** — Python은 `TaskGroup`으로 이걸 언어가 보장하지만, TypeScript/JavaScript는 같은 문제를 겪으면서도(좀비 태스크가 실제로 관찰됩니다) 아직 언어 차원의 해법이 없습니다.

이 책의 핵심 주장 — "우리가 매일 쓰는 비동기 도구들은 사실 함수형 프로그래밍의 오래된 개념 위에 서 있다" — 은 파이썬만의 이야기가 아니었습니다. 다만 이 장이 동시에 보여준 건, **같은 패턴이 언어마다 다른 "보장 수준"으로 구현된다**는 것입니다. Functor/Monad/Applicative라는 대수적 구조는 언어를 넘어 보편적이지만, 그 구조가 얼마나 안전하게 언어 차원에서 강제되는지는 각 언어와 런타임의 설계 선택에 달려 있습니다. 13장까지 배운 게 "무엇이 필연적인가"였다면, 이 장이 보여준 건 "무엇이 아직 선택의 영역으로 남아 있는가"입니다.
// end::body[]
