---
title: "3.3. 가변 데이터로 모델링하기 (Modeling with Mutable Data)"
date: 2026-07-10T00:00:00+09:00
draft: false
tags: ["sicp", "lean", "lean4", "scheme", "mutable-state", "ST", "IORef", "sharing"]
categories: ["books"]
description: "SICP 3장 3절의 아이디어(set-car!/set-cdr!로 만드는 가변 리스트, 큐, 테이블)를 Lean 4의 IORef/ST와 순수 함수형 자료구조로 다시 짜 봅니다."
---

_link:../[전체 목차] · 이전: link:../3-2-environment-model-of-evaluation/[3.2. 평가의 환경 모델] · 다음: link:../3-4-concurrency/[3.4. 동시성: 시간이 중요해지다]_

'''

// tag::body[]
[TIP]
.초보자 이정표
====
*선수지식:* 3.1절의 ``IO.Ref``와 3.2절의 공유라는 생각. 파일 맨 위에는 ``import Std``를 넣으세요.

*이번 장의 도착점:* 두 이름이 같은 가변 셀을 가리킬 수 있음을 보고, 변경 가능한 리스트·그래프의 공유와 순환을 모델링합니다.

*핵심 낱말:* 별칭(alias), 공유(sharing), 가변성(mutation), 동일성(identity). 순환 구조와 동등성 판정은 확장 주제이므로 우선 첫 ``IO.Ref`` 예제부터 실행하세요.
====

지금까지 이 시리즈에 등장한 자료구조는 모두 한 번 만들면 다시 손대지 않는 것들이었습니다 — ``List``는 수정되지 않고, 바뀌지 않은 부분을 공유하는 새 ``List``로 대체될 뿐입니다. SICP 3장은 정반대의 개념을 도입합니다 — _내용물_이 그 아래에서 바뀌는 동안에도 유지되는 _정체성_을 가진 객체입니다. 마치 은행 계좌의 오늘 잔액이 계좌를 만들 때 찍혔던 값과 아무 상관이 없어지는 것처럼 말이죠.

Scheme는 ``set-car!``와 ``set-cdr!``를 추가해 이를 구현합니다 — 기존 쌍 내부에 직접 접근해 칸 하나를 제자리에서 덮어써서, 그 쌍에 대한 참조를 가진 다른 모든 코드가 변경을 보게 만드는 원시 연산입니다. Lean의 ``List``에는 그런 문이 없습니다 — ``List.setCar!`` 같은 건 없고, 있을 수도 없습니다. 그런 게 있다면 ``List``를 자유롭게 공유해도 안전하다는 보장 자체가 깨지기 때문입니다. Scheme이 하나의 보편적 접착제에 변경 기능을 덧붙이는 곳에서, Lean은 데이터를 불변으로 유지하고 대신 변경을 ``IO.Ref``나 ``ST``라는 별도의, 명시적으로 타입에 드러나는 효과로 만듭니다 — 함수 시그니처가 그 효과를 허락해야만 쓸 수 있습니다.

'''

== 3.3.1. 가변 리스트 구조 대신 ``IO.Ref``

``set-car!``/``set-cdr!``가 Scheme에 무엇을 가져다주는지, 그리고 Lean이 의도적으로 무엇을 포기하는지를 가장 분명히 보는 방법은, "내용물이 바뀌어도 유지되는 정체성"이라는 같은 동작을 ``IO.Ref``로 직접 만들어보는 것입니다 — 참조 셀 자체의 정체성은 바뀌지 않은 채, 그 안에 담긴 값만 바꿔치기할 수 있는 셀입니다.

[source,lean]
----
def demoRef : IO Unit := do
  let r ← IO.mkRef (1, 2)
  IO.println s!"before: {(← r.get)}"
  r.set (10, 2)
  IO.println s!"after: {(← r.get)}"

#eval demoRef
-- before: (1, 2)
-- after: (10, 2)
----

같은 ``IO.Ref``를 가리키는 두 참조는 실제로 상태를 공유합니다 — 한쪽으로 쓴 값이 다른 쪽에서도 보이며, 이는 같은 쌍에 묶인 두 Scheme 변수가 ``set-car!``의 효과를 공유하는 것과 같습니다. 반면 내용이 같더라도 독립적으로 만든 참조 두 개는 서로 다른 셀입니다 — 이는 SICP가 ``(cons x x)``와 ``(cons (list 'a 'b) (list 'a 'b))``를 구분하는 것과 정확히 같은 구분입니다.

[source,text]
----
 [공유(Sharing / 별칭) vs 독립된 복사본(Independent)]

 1. 공유 (alias1, alias2):         2. 독립 (a, b):
    alias1 ──┐                       a ──────► ┌─────────────┐
             ▼                                 │ IO.Ref [ 0] │
         ┌─────────────┐                       └─────────────┘
         │ IO.Ref [ 0] │
         └─────────────┘                       b ──────► ┌─────────────┐
             ▲                                 │ IO.Ref [ 0] │
    alias2 ──┘                                 └─────────────┘
----

[source,lean]
----
def sharingDemo : IO Unit := do
  let shared ← IO.mkRef 0
  let alias1 := shared
  let alias2 := shared
  alias1.set 99
  IO.println s!"alias2 sees: {(← alias2.get)}"

  let a ← IO.mkRef 0
  let b ← IO.mkRef 0
  a.set 99
  IO.println s!"b still: {(← b.get)}"

#eval sharingDemo
-- alias2 sees: 99
-- b still: 0
----

[TIP]
.``IO.Ref``에는 정체성을 비교하는 ``eq?``가 없다
====
Lean에는 사용자 코드에서 ``IO.Ref``의 정체성을 직접 비교하는 ``eq?``가 없습니다 — 참조 정체성은 평범한 Lean 값이 노출하도록 의도된 것이 아닙니다. 그것을 노출하면 순수한 Lean 코드를 검증하기 쉽게 만들어주는 "치환에 의한 추론"이 무너지기 때문입니다. 실무에서 "이 둘이 같은 가변 셀인가"를 물어야 한다면, 언어 차원의 포인터 검사에 기대는 대신 생성 시점에 고유한 ``Nat``을 태그로 붙이는 식으로 그 질문을 데이터 모델에 명시적으로 설계해 넣습니다.
====

[NOTE]
====
언어 차원에서 정체성을 감추는 것과, 컴파일러가 실제로 그 값을 어떻게 실행하는지는 별개의 이야기입니다. Lean 4의 컴파일러는 참조 횟수(reference counting) 기반으로 메모리를 관리하는데, 어떤 값의 참조 횟수가 1이라는 것(즉 그 값을 가리키는 곳이 지금 이 자리 하나뿐이라는 것)을 컴파일 시점에 알 수 있으면, "새 값을 만들어 반환"하는 순수 함수형 코드를 실제로는 그 자리에서 메모리를 덮어쓰는 파괴적 갱신(destructive update)으로 컴파일합니다. ``List.map``이나 위에서 만든 순수 큐가 겉보기엔 매번 새 구조를 만드는 것처럼 보여도 실무에서 ``IO.Ref``만큼 빠른 경우가 많은 이유가 이것입니다 — 불변성은 타입과 추론의 층위에서 지켜지는 약속이고, 그 아래 실행 층위에서는 참조 횟수가 허락하는 한 조용히 제자리 갱신이 일어납니다.
====

*연습문제 3.16 (Lean 버전).* SICP는 "쌍이 세 개인 구조인데도 순진한 ``count-pairs``가 3, 4, 7을 반환하거나 영영 멈추지 않는" 예시들을 손으로 그려보라고 합니다. Lean에서는 ``List``가 공유·순환을 표현할 수 없는 트리 구조이므로 이 문제 자체가 원천 봉쇄됩니다 — ``List α``나 link:../2-2-hierarchical-data-and-closure/[2.2절]의 ``Tree α`` 값은 항상 유한하고 순환이 없다는 것이 타입의 불변식이기 때문입니다. 순환이 있는 구조를 표현하려면 셀들이 서로를 가리키게 만들어야 하는데, 여기서 Lean의 제약을 하나 더 만납니다 — ``next : Option (IO.Ref Cell)``처럼 자기 타입을 ``IO.Ref`` _너머로_ 참조하는 귀납적 정의는 Lean의 커널이 거부합니다(``IO.Ref``는 커널이 들여다볼 수 있는 귀납적 타입이 아니기 때문입니다). 그래서 참조를 언어의 포인터 대신 데이터로 명시합니다 — 위 TIP에서 말한 "정체성을 데이터 모델에 설계해 넣는" 방식 그대로, 셀마다 고유한 id를 주고 "다음 셀"은 id로 가리키며 셀들의 저장소(아레나)를 따로 둡니다. 그 순간 "몇 개의 서로 다른 셀이 있는가"라는 질문은 더 이상 구조를 세는 문제가 아니라 어떤 id를 이미 방문했는지 추적하는 문제가 됩니다.

[source,text]
----
 [순환 아레나 구조 (Arena: Std.HashMap Nat Cell)]

   Arena Map:
    id 0: Cell { id := 0, next := some 1 } ──► id 1: Cell { id := 1, next := some 2 }
      ▲                                                                         │
      │                                                                         ▼
      └────────────────────────────────────── id 2: Cell { id := 2, next := some 0 }
----

[source,lean]
----
structure Cell where
  id : Nat
  next : Option Nat  -- 다음 셀의 id

abbrev Arena := Std.HashMap Nat Cell

partial def countDistinct (arena : Arena) (visited : IO.Ref (List Nat)) : Option Nat → IO Nat
  | none => pure 0
  | some id => do
    if (← visited.get).contains id then
      pure 0
    else
      visited.modify (id :: ·)
      match arena.get? id with
      | none => pure 0
      | some c => do
        let restCount ← countDistinct arena visited c.next
        pure (1 + restCount)

#eval show IO Nat from do
  -- 순환 구조: 셀 0 → 1 → 2 → 다시 0
  let arena : Arena := Std.HashMap.ofList [(0, ⟨0, some 1⟩), (1, ⟨1, some 2⟩), (2, ⟨2, some 0⟩)]
  let visited ← IO.mkRef []
  countDistinct arena visited (some 0)
-- 3
----

``countDistinct``에 ``partial``이 붙은 이유를 짚어볼 만합니다 — 순환 구조를 순회하는 함수는 애초에 "인자가 항상 작아진다"는 구조적 재귀의 전제를 만족시킬 수 없습니다(다음 id가 이전 id와 같을 수 있으므로). 방문 목록으로 종료를 보장하는 것은 맞지만, 그 보장은 Lean의 termination checker가 자동으로 볼 수 있는 형태가 아니라 프로그래머가 책임지는 불변식입니다 — 그래서 ``partial``로 이 재귀가 "믿고 맡기는" 재귀임을 명시합니다. 실제로 위 ``#eval``의 순환 아레나(0 → 1 → 2 → 0)에서도 방문 목록 덕분에 3에서 멈춥니다.

'''

== 3.3.2. 큐를 표현하기

SICP는 셀 사슬의 앞쪽을 가리키는 포인터와 뒤쪽을 가리키는 포인터, 이렇게 포인터 한 쌍으로 stem:[\Theta(1)] 삽입이 되는 큐를 만듭니다 — 리스트 끝을 찾아 전체를 훑는 대신 마지막 셀의 ``cdr``을 제자리에서 바꿔서 상수 시간 삽입을 달성합니다. Lean의 ``List``는 변경 없이는 이 트릭을 쓸 수 없습니다 — 불변 단일 연결 리스트의 끝에 원소를 추가하는 것은 어쩔 수 없이 stem:[\Theta(n)]이기 때문입니다. 마지막 ``cons`` 셀에 "도달"해서 다시 쓸 방법이 없고, 설령 도달했다 해도 덮어쓸 수 있는 것이 없습니다.

Lean에서 SICP 큐의 점근 성능을 되살리는 정직한 방법은 두 가지이고, 이 둘이 이 절 전체의 두 극단을 보여줍니다. 첫 번째는 변경을 그대로 유지하는 방법입니다 — 다만 연습문제 3.16에서 본 것과 같은 커널 제약(자기 타입을 ``IO.Ref`` 너머로 참조할 수 없음) 때문에, 셀 사슬 자체는 3.16과 같은 id 아레나로 표현하고, 아레나와 앞/뒤 포인터를 ``IO.Ref``에 담습니다. 삽입은 현재 뒤쪽 셀의 "다음" 칸을 제자리에서 덮어씁니다 — SICP가 ``set-cdr!``로 하는 일 그대로입니다.

[source,lean]
----
structure MutQueuePtr (α : Type) where
  cells : IO.Ref (Std.HashMap Nat (α × Option Nat))  -- id ↦ (값, 다음 셀의 id)
  nextId : IO.Ref Nat
  front : IO.Ref (Option Nat)
  rear : IO.Ref (Option Nat)

def MutQueuePtr.new : IO (MutQueuePtr α) := do
  pure { cells := ← IO.mkRef ∅, nextId := ← IO.mkRef 0
         front := ← IO.mkRef none, rear := ← IO.mkRef none }

def MutQueuePtr.insert (q : MutQueuePtr α) (item : α) : IO Unit := do
  let id ← q.nextId.get
  q.nextId.set (id + 1)
  q.cells.modify (·.insert id (item, none))
  match ← q.rear.get with
  | none =>
    q.front.set (some id)
    q.rear.set (some id)
  | some rearId =>
    q.cells.modify fun cs =>
      match cs.get? rearId with
      | some (v, _) => cs.insert rearId (v, some id)
      | none => cs
    q.rear.set (some id)

def MutQueuePtr.frontItem (q : MutQueuePtr α) : IO (Option α) := do
  match ← q.front.get with
  | none => pure none
  | some id => pure ((← q.cells.get).get? id |>.map (·.1))

def MutQueuePtr.delete (q : MutQueuePtr α) : IO Unit := do
  match ← q.front.get with
  | none => pure ()
  | some id =>
    let next := (← q.cells.get).get? id |>.bind (·.2)
    q.cells.modify (·.erase id)
    q.front.set next
    if next.isNone then q.rear.set none

def queueDemoPtr : IO Unit := do
  let q ← MutQueuePtr.new (α := Char)
  q.insert 'a'
  q.insert 'b'
  IO.println s!"front: {← q.frontItem}"
  q.delete
  IO.println s!"front after delete: {← q.frontItem}"

#eval queueDemoPtr
-- front: (some a)
-- front after delete: (some b)
----

``insert``가 뒤쪽 셀의 "다음" 칸을 직접 덮어쓰므로 리스트를 훑거나 뒤집는 과정이 전혀 없습니다 — SICP의 포인터 변경 그대로, 매 삽입이 (아레나 ``HashMap``의 상각 기준으로) stem:[\Theta(1)]입니다.

두 번째 방법은 앞/뒤 셀의 포인터 정체성을 포기하고, 큐 전체를 평범하고 불변인 리스트 쌍으로 표현합니다 — 큐 자체 안에는 ``IO.Ref``가 전혀 없습니다.

[source,text]
----
 [투 리스트 (Banker's) 큐 구조: O(1) 상각 시간]

          front 리스트                     rear 리스트 (역순 삽입)
      ┌───┬───┬───┐                     ┌───┬───┐
      │ a │ b │ c │                     │ e │ d │  (최신 삽입 원소 e)
      └───┴───┴───┘                     └───┴───┘
        │                                 ▲
        ▼ (delete: O(1))                  │ (insert: O(1))
       출력                             새 원소 입력

  * front가 비면 rear.reverse를 새 front로 승격 (O(n)이지만 상각 O(1))
----

[source,lean]
----
structure MutQueue (α : Type) where
  front : List α
  rear : List α

def MutQueue.empty : MutQueue α := ⟨[], []⟩

def MutQueue.isEmpty (q : MutQueue α) : Bool := q.front.isEmpty && q.rear.isEmpty

def MutQueue.insert (q : MutQueue α) (item : α) : MutQueue α :=
  { q with rear := item :: q.rear }

def MutQueue.frontItem (q : MutQueue α) : Option α :=
  match q.front with
  | x :: _ => some x
  | [] => q.rear.reverse.head?

def MutQueue.delete (q : MutQueue α) : MutQueue α :=
  match q.front with
  | _ :: xs => { q with front := xs }
  | [] => match q.rear.reverse with
    | [] => q
    | _ :: xs => { front := xs, rear := [] }

def queueDemo : IO Unit := do
  let ref ← IO.mkRef MutQueue.empty
  ref.modify (·.insert 'a')
  ref.modify (·.insert 'b')
  IO.println s!"front: {(← ref.get).frontItem}"
  ref.modify (·.delete)
  IO.println s!"front after delete: {(← ref.get).frontItem}"

#eval queueDemo
-- front: (some a)
-- front after delete: (some b)
----

이것이 바로 고전적인 _투 리스트(Banker's) 큐_입니다 — ``insert``는 ``rear``에 stem:[\Theta(1)]로 붙이고, ``delete``는 ``front``에서 stem:[\Theta(1)]로 꺼냅니다. 예외는 ``front``가 바닥났을 때뿐인데, 이때는 ``rear``를 뒤집어 새 ``front``로 만드는 데 한 번 stem:[\Theta(n)] 비용을 치릅니다. 이 비용은 실재하지만 긴 연산 시퀀스에 걸쳐 _상각(amortize)_됩니다 — 이것이 순수 불변 표현으로도 SICP의 포인터 변경 수준의 성능을 얻는 표준적인 트릭입니다. ``MutQueuePtr``처럼 큐의 정체성이 호출 사이에 유지될 필요가 없다_면_ ``IO.Ref``는 큐 내부에는 아예 필요 없습니다(그런데도 위 ``queueDemo``가 여전히 ``IO.Ref``로 감싸는 이유는, 큐 자신의 로직은 순수하더라도 여러 호출 지점에 걸쳐 하나의 자라나는 큐를 공유하려면 "지금의 큐"를 담아둘 자리가 필요하고, 그 자리가 곧 참조이기 때문입니다).

위의 ``insert``/``delete`` 함수는 아예 재귀하지 않으므로 종료성 질문이 나올 자리가 없습니다. 재귀가 등장하는 유일한 곳은 ``frontItem``/``delete`` 안의 ``List.reverse``인데, 이는 항상 줄어드는 리스트에 대한 평범한 구조적 재귀입니다 — ``partial``도 ``termination_by``도 필요 없습니다.

'''

== 3.3.3. 테이블을 표현하기와 메모이제이션

SICP는 헤더가 달린 가변 연관 리스트로 테이블을 만듭니다. 그래야 ``insert!``가 새 레코드를 stem:[\Theta(1)]로 앞에 이어붙이면서도, 호출자가 "그 테이블"을 가리키는 새 포인터를 받을 필요가 없습니다 — 헤더 셀의 정체성이 곧 테이블의 정체성입니다. Lean 생태계에는 이 절이 손으로 만들고 있는 도구가 이미 있습니다 — ``Std.HashMap``은 (겉보기엔 가변적이지만 내부적으로는 영속 구조로 구현된) stem:[\Theta(1)] 상각 조회·삽입을, 손으로 만든 백본-헤더 트릭 없이 제공합니다.

[source,lean]
----
def buildTable : Std.HashMap String Nat :=
  (∅ : Std.HashMap String Nat) |>.insert "a" 1 |>.insert "b" 2 |>.insert "c" 3

#eval buildTable.get? "b"
-- some 2

#eval buildTable.get? "z"
-- none
----

SICP가 테이블에 제시하는 가장 설득력 있는 동기는 메모이제이션입니다 — 인자를 키로 삼아 함수가 이전에 계산한 결과를 캐싱하는 것으로, 순진한 지수 시간 피보나치가 가장 극적인 예입니다. Lean에서 정직한 메모이즈드 함수는 ``IO.Ref``(또는 ``StateM``)가 필요합니다 — 캐시를 갱신하는 것 자체가 변경이기 때문입니다. 이전에 어떤 인자로 호출됐는지 몰래 기억하는 ``Nat → Nat`` 함수는 사실 숨겨진 부작용을 가진 함수이고, Lean의 타입 시스템은 바로 그 숨김을 선택 사항이 아니게 만들도록 설계되어 있습니다.

[source,lean]
----
def mkMemoFib : IO (Nat → IO Nat) := do
  let cache ← IO.mkRef (∅ : Std.HashMap Nat Nat)
  let rec fib (n : Nat) : IO Nat := do
    match (← cache.get).get? n with
    | some v => pure v
    | none => do
      let result ← if n = 0 then pure 0
                   else if n = 1 then pure 1
                   else do
                     let a ← fib (n - 1)
                     let b ← fib (n - 2)
                     pure (a + b)
      cache.modify (·.insert n result)
      pure result
  pure fib

def memoFibDemo : IO Unit := do
  let fib ← mkMemoFib
  IO.println (← fib 30)

#eval memoFibDemo
-- 832040
----

내부 ``fib``은 ``n``에 대해 구조적으로 재귀하며, 평범한 함수로 써도 Lean의 종료성 검사기를 그대로 통과했을 것입니다 — 이 함수가 ``do`` 표기법 안에서 ``IO``를 통해 쓰이는 이유는 종료성 때문이 아니라, ``cache``를 읽고 쓰는 것이 모든 호출의 타입에 드러나야 하는 효과이기 때문입니다. 이 시리즈 첫 글의 순수하고 메모이제이션 없는 ``fib``과 비교해보세요 — 재귀 형태도, 종료성 논증도 같지만 시그니처 어디에도 ``IO``가 없습니다. 호출 사이에 아무것도 기억할 필요가 없었기 때문입니다.

*연습문제 3.27 (Lean 버전).* SICP는 ``(memoize fib)``처럼 이미 정의된 순수 재귀 ``fib``을 감싸는 것과, ``memo-fib``이 자기 자신을 재귀 호출하도록 처음부터 다시 쓰는 것의 차이를 물어봅니다. Lean에서 이 차이는 타입에 그대로 드러납니다 — 위 ``mkMemoFib``의 내부 ``fib``은 재귀 호출 ``fib (n - 1)``이 _같은_ 캐시를 참조하는 ``IO``액션이기 때문에 stem:[\Theta(n)]입니다. 만약 순수 ``def fib : Nat → Nat``(이 시리즈 1.2절의 그 함수)을 밖에서 감싸는 ``memoize``를 만든다면, 캐시는 최상위 호출 하나만 기억할 뿐 그 함수가 자기 자신을 호출할 때 만드는 재귀 호출들은 여전히 캐시를 모르는 순수 ``fib``을 타므로, 여전히 지수 시간이 됩니다 — 메모이제이션의 효과를 보려면 재귀 구조 자체가 캐시를 아는 버전을 참조해야 한다는 것이 핵심입니다.

'''

SICP 3.3.4절(이벤트 기반 디지털 회로 시뮬레이터)과 3.3.5절(제약 전파 시스템)은 각각 완결된 큰 시스템이라, 여기서 급하게 요약하기보다 이 시리즈의 범위 밖으로 남겨둡니다. 이번 글에서 계속 이어온 핵심은 이것입니다 — SICP의 ``set-car!``/``set-cdr!``는 "유지되는 정체성"과 "실제 변경"이라는 두 역할을 동시에 하지만, Lean은 이 둘을 갈라놓습니다 — 정체성은 ``IO.Ref``(또는 명시적 id)가, 그리고 변경이 허용되는 곳과 허용되지 않는 곳을 정확히 추적하는 일은 타입 시스템이 맡습니다.

'''

== 정리

[cols="1,1,1",options="header"]
|===
|SICP 개념 |Scheme |Lean 4
|가변 쌍 |``set-car!``/``set-cdr!`` |``IO.Ref`` 안의 값 교체 (``List``에는 없음)
|공유와 별칭 |같은 쌍을 가리키는 두 이름 |같은 ``IO.Ref``를 가리키는 두 바인딩
|순환 구조 |``set-cdr!``로 즉석 생성 |``IO.Ref`` 중첩은 커널이 거부 — id 아레나로 명시
|Θ(1) 큐 |앞/뒤 포인터 + 포인터 변경 |id 아레나 큐(가변) 또는 투 리스트 큐(불변·상각)
|테이블 |가변 연관 리스트 + 헤더 |``Std.HashMap``
|메모이제이션 |테이블에 ``insert!`` |캐시 ``IO.Ref`` — 효과가 타입에 드러남
|===

다음 글에서는 3.4절 — 여러 프로세스가 같은 상태를 공유할 때 시간이 왜 본질적 문제가 되는지, 그리고 시리얼라이저와 뮤텍스 — 를 다룹니다.
// end::body[]
